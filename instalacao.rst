Installation
==========

To install the project dependencies, use:

.. code-block:: bash

    pip install -r requirements.txt

Make sure to set up environment variables as needed (e.g., .env).

To run the API:

.. code-block:: bash

    python api/main.py

To start the Streamlit app:

.. code-block:: bash

    streamlit run streamlit_app/app.py